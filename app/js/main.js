window.addEventListener('DOMContentLoaded', function() {

    window.ui = new vicmd.UI('#main');
    vicmd_ui.ready();

    // window.addEventListener('keydown', function(e) {
        // console.log(e);
        // vicmd_ui.log(e.keyCode);
        // vicmd_ui.log(e.keyIdentifier);
    // });

});
